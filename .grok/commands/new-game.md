执行 bunkiten skill 的【新开局】流程。

若当前世界（缺省 `main`；外部指令指定世界 id 时以它为准，见 skill 的【世界线】）的 `state/worlds/<worldId>/state.md` 已存在：先确认——继续上局 / 重开同剧本 / 换剧本（优先一张选项卡；卡片工具不可用时按文本选项降级）。选择重开时，把旧文件改名为同目录下的 `state.bak.md` 与 `summary.bak.md` 再初始化，并提供"继承世界观、重置好感度与主线"的二周目选项；需要全新世界线时，改到 `state/worlds/<新id>/` 初始化，旧世界原样保留。

若不存在：直接走【新开局】。

# 剧情树是骨架，不是铁轨

一章的剧情树预先完整生成，但它约束演出的方式是"骨架"：正常沿节点与边行进；玩家自由输入偏离时即兴演出并把结果嫁接为新枝；不可达枝剪枝（标记不删除）。我们否决了"铁轨"方案（严格沿边行进、自由输入只映射最近边）——它会废掉产品核心的 galgame 自由输入体验，也让"动态裁剪剪枝"失去意义。树规模纪律（12-20 节点、2-3 汇合点）和嫁接机制是这一定价的代价与配套。

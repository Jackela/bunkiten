# 每章全分支全量美术预生成

每章开演前，按剧情树枚举本章全部地点与全部角色（含玩家永远不会走的分支专属项）一次性生成完毕，正片期间零美术等待。代价是约 30-50% 的冗余生成成本（每章约 $0.3-1 的 Imagine 花费与 8-20 分钟章间等待）；换来的是"等待集中、体验绝不打断"的产品承诺。备选方案"必然项全量 + 分支项按需"被否决，因为它把等待重新引入正片。assets 按名缓存保证二周目不重复花钱。

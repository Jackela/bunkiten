# bunkiten 协作导航

LLM 互动 AVG（文字冒险 / 视觉小说）：Electron 壳 + React 前端 + `grok agent` stdio（ACP）做叙事引擎。引擎是 prompt（skill），剧本是数据，进度是文件。

## 项目地图

| 位置 | 是什么 |
|---|---|
| `.grok/skills/bunkiten/SKILL.md` | 引擎全部真相：每轮协议（含【立绘】表情切换行与音频三行）、客户端开局指令、章节与剧情树、美术与预载、素材重绘、剧本创作（创作模式）、音频（曲/环境/音效）、导演层、状态纪律 |
| `.grok/commands/` | 元命令定义：/new-game /recap /presets /help |
| `server/` | v1.7 拆成入口 + 10 模块（`acp-server.mjs` 头部注释带模块地图）。入口 `acp-server.mjs`（653 行）= `startServer` 闭包装配（资产落盘流水线 / 逐轮快照与回合日志落盘点 / 质量守卫 `supplementMissingOptions` / `sniffPreset` 世界与剧本落定 / 端口重试与 SIGTERM 优雅退出）+ 纯函数 `EFFORT`/`EFFORT_PLANNING`/`pickEffort`/`isMainTurn`/`parseWorldRef`/`isDirectivePrompt` + 契约 lint ⑥ 钉面（函数体必须引用 shared 真源的 `DIRECTIVE_PREFIX_RE`/`CHAPTER_MARK_RE`；`FONT_PRESETS`/`DIALOG_TEXTURES`/`DEFAULT_THEME` 三个 theme 字面量与 `src/theme.ts` 比对——**钉在入口源码**，`presets.mjs` 反向 import 且仅函数体内引用（ESM 环形安全））+ 拆出符号逐名 re-export（electron/tests/doctor 的 import 面逐字不变）。模块：`protocol-lines.mjs`（`RULES` 原文、`SUPPLEMENT_PROMPT`、五种协议行 parse*）、`routes.mjs`（`createRequestHandler(ctx)` HTTP 路由链 + `/app` 静态托管，闭包能力由入口注入）、`acp.mjs`（`createAcpSession` 工厂：spawn/JSON-RPC/sessionId/boot/会话图片定位，接缝只有 `onChunk`/`onSeg` 两回调）、`config.mjs`（GAME_ROOT/端口）、`assets.mjs`（资产路径契约纯函数 + `uniqueSuffixedName` 重名后缀（worlds/presets 导入共用）+ shared 的 `ASSET_FILE_RE`/`ASSET_KINDS` re-export）、`presets.mjs`（preset 解析 + 剧本导出包 `buildPresetBundle`/`importPresetBundle` 纯函数，routes 只做薄壳）、`snapshots.mjs`（三文件与快照地基（`TREE_FILE`/`WORLD_FILES`）、`writeTurnLog`、`readSnapshots` 列表缓存：**逐文件**名字+mtime 复用（只有新增/被覆盖的文件才解析）、出口浅拷贝）、`worlds.mjs`（索引/CRUD/导出导入/`moveToTrash` 回收站/角色面板 `stateViewFor`）、`audio.mjs`（`scanPresetAudio`）、`http-util.mjs`（跨站 403、`readBodyText` 可选 `maxBytes`——缺省 5MB、剧本导入端点独享 50MB、MIME）；会话图索引（跨会话同名取 mtime 最新、5s TTL）在 `acp.mjs` 的 `resolveImage` |
| `shared/protocol.mjs` | 协议常量唯一真源（v1.7，ADR-0012）：`PROTOCOL_HEADS`（9 头）、`AUDIO_KINDS`/`AUDIO_EXTS`/`AUDIO_MIME`/`AUDIO_FILE_RE`/`AUDIO_REL_RE`（音频白名单与直服正则，后两者由前两者构造）、`ART_KINDS`/`ASSET_KINDS`/`ASSET_FILE_RE`（美术类型字面与资产文件名正则，v1.7 收尾收编）、`DIRECTIVE_PREFIX_RE`（指令前缀，`pickEffort` 推理分档与 `isMainTurn` 正戏回合判定共用；「待命：」后缀判定不在此）、`CHAPTER_MARK_RE`（章标记）。零依赖纯常量：`src/lib/parser.ts` import + re-export（公共 API 逐字不变）、`server/acp-server.mjs` import（并 re-export 音频/资产常量给 doctor），`shared/protocol.d.mts` 手写声明供 `tsc -b`（.mjs→.d.mts），vite/vitest/electron 运行时直接吃 .mjs；打包进 asar（electron-builder `files: shared/**`）。`RULES` 刻意不收编——引擎只读 `.grok/` 提示词不 import 代码，server↔SKILL.md 双份 + lint 逐字比对仍是正确机制 |
| `scripts/doctor.mjs` | 剧本体检查（v1.7，作者侧 CLI `npm run doctor`）：核心是可导入纯函数 `checkPreset(presetDir, root)`/`checkAllPresets(root)`（tests/doctor.test.ts 的 seam；解析口径 import server 的 `parseFrontmatter`/`parseCharacterSections`/`parseSectionLines`/`normalizeTheme`/`FM_KEYS`/`THEME_KEYS`/`PRESET_ID_RE`/`AUDIO_KINDS`/`AUDIO_EXTS` 与 `src/theme.ts` 的 `HEX_RE`/`MOTIFS`/`FALLBACK_THEME`——.ts 由 Node ≥23.6 type stripping 直 import，不抄第二份）+ `main()` 打印壳。七组检查：frontmatter 必填键（id/title 缺=进不了轮播、tagline/genre/rating 缺=标题屏栏位为空，都 error）与 id=目录名（error）、theme 双层回退预警（server 的 normalizeTheme + 客户端更严的 6 位 hex/四母题闭集，warn）、`# 主要角色`/角色节（error）与建议字段 `art_prompt`/`agenda`、`# protagonist_card` 缺失（warn）、cover（只认 `cover.jpg`，缺失或只有 `.jpeg` → warn）、assets 文件名契约（error）与孤儿素材（warn，引用面=全部世界 state.md ∪ 全部 preset.md 文本，trash 不算）、audio 文件名契约（error）与 `类型-名` 重复（warn）。退出码非 0 ⟺ 有 error；**不进 CI**（作者侧按需跑——preset 健康度随游玩数据漂移，CI 已有单测守纯函数） |
| `electron/main.js` | 主进程：GAME_ROOT 定位、PATH 补齐、启动 acp-server、开窗口、打包态查更新（electron-updater，开发态与无 `app-update.yml` 的产物静默跳过） |
| `electron-builder.yml` | 打包布局（extraResources → `resources/game/` 与 `app-dist/`）、双平台目标、`publish: github`（更新通道前提）、`afterSign: electron/notarize.cjs` 公证钩子与 mac 签名/entitlements（`build/entitlements.mac.plist`） |
| `src/lib/parser.ts` | 文本协议纯函数：开局（含世界段）/美术/重绘/章节规划/创作模式/续玩/剧情编辑指令构造、**行动** 解析与截断、【图】【清单】【章】【立绘】【新剧本】【树】【曲】【环境】【音效】协议行扫描（`PROTOCOL_HEADS` re-export 自 `shared/protocol.mjs` 唯一真源、`isProtocolLine`/`ART_LINE_BODY`/制作清单正则都由真源构造，`ArtKind` 类型源 = `ART_KINDS`）与差分名拆分、`parseStoryTree` 容错解析（契约字符串所在） |
| `src/store/` | 状态机（v1.6 拆成切片）：`game.ts` 只是组装点（初始 state、`isTypingTarget`、按序 `{...slice(ctx)}` 拼 store、公共 API 再导出），功能块在 `slices/{nav,world,tree,assets,creation,crafting,gameplay,characters}.ts`，跨片共享闭包与 watchdog/autoAdvance 定时器单例在 `context.ts`，字段类型在 `types.ts`、立绘纯函数在 `portrait.ts`。职责不变：段过滤、标记→画面、表情切换（【立绘】→差分）、选项→按钮、章节制作流水线与【章】切章、画廊重绘/批量挂起、创作模式装配、世界线（继续/新世界）/剧情图/设置 overlay、剧本导入（v1.7 nav 片：`parsePresetBundle` 本地校验 → `importPresetText` POST + 重取轮播，提示落 `titleNotice`）、`awaitCommand` 切屏、`toWorlds()`（捏人屏的「返回」按钮与 Esc 都走它：只切屏回世界线屏，牌面不借道 `resetRunState`、原样留着）、重掷本回合（`rerollTurn`：restore 次新 turn 快照 + 重同步收尾后重发同一玩家输入，`lastTurnPrompt`/`pendingTurnPrompt`/`pendingRerollPrompt` 三字段记账；按钮可见性判据 `turnSnapshots`——建新世界 0、入场与「未知或 <2」的回合收尾经 `context.refreshTurnSnapshots` 按需补拉 /api/history，数够即停）、角色面板抽屉（`characters.ts`：开合 + `/api/state` 取数，turn_end 面板开着重拉、`resetRunState` 清空）；组件与测试一律从 `store/game` import（公共 API 逐字未变） |
| `src/lib/acp.ts` | HTTP/SSE 客户端与 `AcpEvent` / `Preset` / `WorldEntry` / `WorldSnapshot` / `WorldBundle` / `PresetBundle` / `AudioItem` / `StateView` 类型，`fetchWorlds`/`postWorld`/`fetchTree`（世界线与剧情图屏）、`fetchHistory`/`fetchSnapshot`/`worldExportUrl`/`postWorldImport`/`postWorldUpdate`（快照索引与单条全文、导出导入、改名）、`presetExportUrl`/`postPresetImport`（剧本导出下载与导入，v1.7）、`fetchAudio`/`audioFileUrl`（音频索引与直服 URL）、`fetchState`（角色面板的 state.md 解析视图） |
| `src/lib/audio.ts` | `AudioManager` 单例（v1.6，挂在 React 之外）：`setPreset`/`handle`/`stopAll`/`applySettings`；BGM 与环境各一对音频元素交叉淡入 ≈600ms（`FADE_MS`）、音效一次性（并发 ≤4、8s 兜底超时）、按 preset 记账（在途/就绪/已判定失败都不重拉）、索引里查不到「类型\|名」即静默 `console.debug` |
| `src/lib/settings.ts` | 设置模型与持久化（v1.6）：localStorage 键 `bunkiten.settings.v1`；`GameSettings`（master/bgm/ambient/sfx/muted/textSpeed/autoAdvance）、`DEFAULT_SETTINGS`、`normalizeSettings`（非法值逐键回默认，不迁移）、`loadSettings`/`saveSettings`、`TEXT_SPEED_MS`；设置是本机偏好，**不进世界线** |
| `src/lib/treeLayout.ts` | 剧情树布局纯函数：最长路径分层 + 抗环 + 确定性坐标（`layoutTree`/`LayoutNode`/`LayoutEdge`）+ 视图函数（`fitView`/`zoomViewAt`/`panView`/`viewBoxOf`），StoryTreeScreen 的 SVG 树图数据源 |
| `src/lib/genealogy.ts` | 世界线家谱布局纯函数（v1.7）：forkedFrom 森林分层 + 孤儿（父线已删，`missingParent`）与 fork 环容错 + 确定性整数坐标（`layoutGenealogy`/`GenealogyLayout`，层内 lastPlayed 降序）、圆角拐弯边路径，`genealogyStep` 给家谱键盘走位的确定性规则，WorldsScreen 家谱视图的 SVG 数据源 |
| `src/lib/diff.ts` | 快照对比纯函数（v1.7）：行级 LCS `diffLines(a, b)`（O(n·m) DP，add=b 新增、remove=a 独有，替换块 remove 在 add 前；空串/undefined 容错 0 行、尾部换行不算行）与 `diffStats`（+N −M 摘要），StoryTreeScreen 快照对比面板的数据源 |
| `src/lib/status.ts` | 引擎状态文案 → 玩家口吻的唯一映射：`playerStatus(status)` 纯函数（「引擎演绎中…」→「故事展开中…」、「撰写章节大纲…」→「章节筹备中…」、`出错：X` → `出错了：X`，表外状态原样透传），游戏屏顶栏、制作中屏与 App 的 `StatusAnnouncer`（aria-live 播报）三处共用——store 里的原字符串一个字不动 |
| `src/lib/worlds.ts` | 世界线展示层兜底：`LEGACY_FORK_NOTE_RE`/`isLegacyForkNote(note)` 把旧版 server 自动写进 `index.json` 的分叉备注（「分叉自 <id> @ <节点>（精确快照 #N）」）判为「没有备注」——`note` 会被当世界名显示，等于把裸 id 摊给玩家；血缘另有 `forkedFrom` 与家谱连线，显示层不靠这句话还原 |
| `src/theme.ts` | 剧本主题（accent/accent2/motif + v1.7 font 字体族/dialog 对话框质感）逐键兜底与 `--accent`/`--accent2`/`--font-preset` CSS 变量注入；`FONT_STACKS` 系统字体栈（mac/win + Linux Noto 兜底，离线无 webfont）、`dialogClass` 映射对话框质感类 |
| `src/components/` | 屏幕流 boot→title→worlds→protagonist→(crafting)→game 各屏与游戏 HUD（壳层屏吃 global.css 的 `.shell-backdrop` 满幅底，其中 worlds/protagonist/crafting/settings/assets 五屏共用 `ShellPage` 页框，见下一行；title 是卡带舞台、creation/story-tree 各有版式，都不走页框），外加 overlay 屏 AssetsScreen（画廊：分组/inUse/大图/单项重绘 + 选择模式批量重绘（顺序队列）与批量删除）、CreationScreen（创作模式：打磨对话→装配清单→新剧本入轮播）、StoryTreeScreen（剧情图：SVG 树图+节点详情+`剧情：`自然语言编辑+在此分叉带快照 seq+存档点标注与原地回退+与上一个存档点对比（`prevSnapshotSeq` 基线含 backup，三 tab diff、equal 折叠 ±2 行上下文）；>40 节点降级列表；滚轮锚点缩放/拖拽平移；顶部章节切换器列出解析出的每一章、带进度指针的那章标「当前」，点谁画谁；`归档` 药丸只是目录信息，点它回一句「这一章已归档，只保留了目录信息」；节点详情 ≥lg 住右栏 380px sticky 自滚、<lg 回到画布下方；玩家侧文案「存档点 · 第 N 幕」，`快照 #N` 与轮/回合不上屏）与 SettingsScreen（设置屏：主音量/静音/BGM·环境·音效三滑杆/文本速度/自动前进，游戏屏命令轨「设置」进入）；WorldsScreen 世界线屏（列表/继续/新世界线/导入/键盘导航；行上收成一个主行动「继续」+ 一个 `⋯` 菜单（改名/导出 `.world.json`/删除，删除的两段确认收在菜单内，Esc 或点菜单外面即关）；行文案不吃裸 id——`worldDisplayName` 回退链 label → note（旧版分叉备注不算）→ 剧本名 → 「未命名世界线」，血缘由 `forkPhrase` 说；家谱视图：列表↔家谱切换、forkedFrom 森林 SVG、孤儿 ⌫ 徽章、方向键走节点 + Enter 选中、详情条吸底（「继续」同样复用 `resumeWorld`）、缩放平移复用 `lib/treeLayout` 的视图纯函数（滚轮指针锚点/拖拽平移/双击与 `±`「适应」/`+ - 0`）、渲染宽度只封上界（单节点 ≈270px）、提示行挂在画布工具条上）、TitleScreen 封面卡带轮播（字标 `bunkiten` + `分岐点`；左下「继续上次」`title-continue` 直通最近游玩的世界线；右下角落簇 = 当前卡「导出」/「导入剧本」/「素材」/「创作新剧本」——导出已从卡面挪进角落簇；卡面按实体卡带分带（标签带/磁带窗/底缘脊柱）；左右宽命中区里放可见的 chevron）、CraftingScreen 制作中屏（规划→美术逐项点亮，状态行经 `playerStatus` 转玩家口吻）、Atmosphere 颗粒/暗角；游戏 HUD：TopBar（左侧只留异常/进行中状态簇——就绪且不忙、无待重同步时整块 `sr-only`，`待重同步` 徽章与「再同步」照常现身；章号已移出顶栏；右侧竖排文字命令轨：设置/历史/角色/画廊/剧情图/重演/重开/前情/换剧本/帮助）、DialogueBox（打字机 + 面板内「自动/快进」控件，自动经 store 的 `resumeAutoAdvance` 武装）、GameStage（对话列 `min(800px,100vw)`，满宽外层带 `.portrait-reserve`——≥lg 给立绘让位）、PortraitLayer（立绘占真实布局盒：grid 单格堆叠 `grid-rows-1`，差分两级回退交叉淡入）、HistoryDrawer（回想抽屉：最新一幕在最上，标题带当前章号、回溯/重演分割线）、CharactersDrawer 角色面板抽屉（游戏屏侧栏：好感度/表情徽章/秘密剧透折叠/幕后手记/线索·未了伏笔） |
| `src/components/ShellPage.tsx` | 壳层页框：worlds/protagonist/crafting/settings/assets 五屏共用的外框（title 是卡带舞台不走页框、creation/story-tree 各有版式，其文件头注释把 title 也算进来，与 import 面不符——以代码为准）——满幅主题底（屏外层吃 global.css 的 `.shell-backdrop`）+ 全仓唯一一处壳层定宽 `max-w-[84rem]` + 统一表头（eyebrow → `text-title` 屏标题 + accent 短分隔线 → 右侧动作簇，窄屏自动换行）+ 可选 ≥xl 360px 右栏（`xl:grid-cols-[minmax(0,1fr)_360px]`，窄屏顺序落到正文之后、不做抽屉）+ 可选页脚提示行。它钉住的那条规则：**壳层屏不再各屏自写居中窄栏**，屏内区块撑满可用宽度、靠栅格与多列组织信息 |
| `src/components/game/ChapterCard.tsx` | 章节过场卡（VN 惯例）：章号一变在画面正中亮一次「第 N 章」，`CHAPTER_CARD_MS` 2200ms 在屏、末尾 `CHAPTER_CARD_FADE_MS` 400ms 淡出（两个常量导出给用例当时间真源；卸载时刻钉死在 setTimeout 上，不用 AnimatePresence，假定时器也推得动）；**挂在 App 常驻层**（不在 GameStage 里），跨屏记忆放组件内 ref——世界线或章号变过才亮（从 overlay 返回不重亮、换世界线重新武装）；`pointer-events-none` 不拦点击、无可聚焦元素不抢焦点、z-40 压在对话区与命令轨之上、抽屉与 overlay 之下 |
| `src/components/motifs/` | motif 氛围层：summer/rune/imperial/aurora 四款纯 CSS 动画图案（`MotifLayer` 分发） |
| `presets/<dir>/preset.md` | 剧本数据：加剧本只改这里，无需动代码；同级 `assets/` 放该剧本的立绘/背景、`cover.jpg` 放封面、`audio/`（可选，作者手放）放该剧本音频 |
| `state/worlds/<worldId>/` | 世界线运行时进度：state.md / summary.md / story-tree.md 三文件 + `history/NNNN.json` 逐轮快照（4 位递增、append-only，存正戏回合的三文件全文）+ `logs/NNNN.json` 回合原文日志（v1.7，与 history/ 平级、append-only、**不进世界线导出包**，条目 `{seq,at,prompt,text}`——正戏回合「发了什么 + 引擎回了什么」的回溯面，见「回合日志与质量守卫」），父级 `state/worlds/index.json` 存元数据（worldId/preset/title/chapterNo/lastPlayed/label/note/forkedFrom），引擎的 SSOT；旧扁平 `state/*.md` 首次启动一次性迁入 worlds/main/。删除不直删：世界线与素材先整体挪进 `state/trash/<ts>-<rand4>[-<剧本id>]-<原名>/`（v1.7 回收站，恢复走 `state/README.md` 手工文档，不自动清理，见 ADR-0014） |
| `presets/<id>/assets/` | 美术资产随剧本走：`<类型>-<名字>.jpg`（差分 `立绘-<角色>-<变体>.jpg`），封面同级 `cover.jpg`；顶层全局 `assets/` 池自 v1.5.1 起不再存在，老存档里的 `assets/<文件>.jpg` 只在当前剧本目录内只读兼容直服（不跨剧本扫描）。音频是**另一条管线**：同级 `presets/<id>/audio/`，不进 `assetRegistry`、`/api/assets` 不列 |
| `presets/<id>/audio/` | 剧本自带音频素材（v1.6，作者手放；rift-mark 自带三个合成示例 wav：`曲-夜灯谣`/`环境-雨夜檐滴`/`音效-翻页`，其余三个 preset 没有此目录 = 静默不播）：`<类型>-<名>.<ext>`，类型 ∈ 曲/环境/音效，ext ∈ mp3/ogg/m4a/wav/flac；server 只扫描（`scanPresetAudio`）与直服，不生成、不落盘、不进 assetRegistry |
| `tests/` | 单测：`parser.test.ts` 契约快照与协议头断言、`crafting.test.ts` 章节制作流水线与 store 公共 API、`server.test.ts` 世界线/协议解析/快照子系统/音频扫描/剧本导出包/角色面板解析、`treeLayout.test.ts` 布局与视图纯函数、`genealogy.test.ts` 家谱布局与键盘步进纯函数、`diff.test.ts` 快照对比行级 LCS 纯函数、`doctor.test.ts` 剧本体检查纯函数（tmp 根造 preset）、`ui.test.tsx` 组件（jsdom）、`preload.test.ts` 立绘差分预热（node 环境，fetch/Image 垫片）、`contract.test.ts` 契约 lint（v1.6 起防漂移门禁：协议头/音频白名单/指令前缀的真源断言（`shared/protocol.mjs`）/RULES 逐字副本/指令字符串双处/主题白名单与兜底主题/用例数与文档比对/设置键，自身不计入 431 口径）；`setup-react-act.mjs` 为 react 19.3 缺失 `React.act` 打测试垫片；`e2e/smoke.spec.ts` 真引擎冒烟；`helpers/stack.mjs` 进程编排；`tests/e2e-ui/`（12 个 spec、共 19 条用例：`opening`（另断言立绘的渲染尺寸与「对话区不压立绘」）/`settings`/`keyboard`/`gallery`/`story-tree`/`worlds`/`restore`/`reroll`/`reduced-motion`/`focus`/`characters`/`presets` + `flow.ts` 屏幕流 helper，`playwright.ui.config.ts` 默认 chromium、不重试）假引擎确定性 UI e2e，进程由 `helpers/fake-stack.mjs` 编排（复用 `tests/integration/harness.mjs` 起假引擎+真 acp-server，再起 vite dev 代理过去；harness 的 seed 项 assets/audioFiles/trees/stateFiles/worlds/snapshots 在此消费） |
| `tests/integration/` | 进程级集成层（v1.6）：假 ACP 引擎（`fake-engine.mjs`，脚本化 `session/update`）+ 临时 PATH 垫片 `grok` + 真 `acp-server.mjs` 子进程（`harness.mjs` 注入 `GROK_GAME_ROOT`/`HOME`/`PORT`，SIGTERM 收尾），秒级、**随 `npm test` 跑**；`pipeline.test.ts`（【图】落盘/路径穿越/【立绘】/【新剧本】/【树】/sniffPreset/API 冒烟/剧本导出导入往返）、`audio-history.test.ts`（音频三行与 `/api/audio`·`/audio`、逐轮快照、精确 fork/restore、导出导入、label/note、素材删除）、`http-guard.test.ts`（跨站 403、body 413、history seq） |
| `vitest.config.ts` | 单测配置：`setupFiles` 加载 react act 垫片、排除 `tests/e2e/**`（e2e 走 Playwright）、钉 `NODE_ENV=test` |
| `docs/ARCHITECTURE.md` | 架构真相：ACP 契约、文本协议契约（含【曲】【环境】【音效】三行）、世界线与状态文件布局（含家谱视图）、逐轮状态快照与精确回退（含回退后语义/重掷/快照对比）、回合日志与质量守卫（v1.7）、世界线导出包、剧本导出包（v1.7）、角色面板（v1.7）、API/SSE 一览、资产与音频管线、剧本体检查（doctor，v1.7）、动效降级与焦点对比度（v1.7）、打包布局与已知限制、修改指引 |
| `docs/adr/0001-0017` | 裁决记录（kebab-case，编号递增）：剧情树骨架、全分支预生成、差分分层、表情由引擎驱动、缓存权威、世界线与分叉、剧情图、资产随故事走、逐轮快照与精确分叉、音频协议、打 tag 即发版、协议单一真源（shared/protocol.mjs）、server 模块化、删除进回收站、回合日志与质量守卫、剧本导出包、a11y 基线 |
| `CONTEXT.md` | 领域词表（术语 → 定义 → _Avoid_ 反例），改术语先改这里 |
| `.github/workflows/ci.yml` | CI：push/PR 跑 `npm ci` + `npm run build` + `npm run typecheck:server` + `npm run test:coverage`（431 例 + 契约 lint + 覆盖率防下滑阈值，替代 `npm test` 单独步骤）+ 上传 coverage 报告 artifact + `npx playwright install chromium --with-deps` + `npm run test:e2e:ui`（假引擎确定性 UI e2e，随 CI 跑；真引擎 e2e 仍不在 CI，需本机登录引擎且能出网 x.ai） |
| `.github/workflows/release.yml` | 发版（v1.6）：push `v*` tag（或手动 dispatch）→ guard 校验 tag == `v<package.json version>` 且 `npm test` 绿 → mac/win 矩阵打包（都 `--publish never`）→ 合并纯 LF `SHA256SUMS.txt` → 建/更新 GitHub Release |
| `docs/releases/` | 每版一篇发布说明 `docs/releases/<tag>.md`；release job 优先拿它当 Release body（其次是 gh api 自动 notes、最后兜底文案） |

历史遗留（`shell/` 网页原型、MVP 期 TUI 的 `play.bat` / `install.ps1` / `START-HERE.txt`）已全部删除：仓库只有 Electron 形态，导航不要再指向它们。

## 契约同步（改协议前必读）

文本协议是一组逐字字符串，散在多处，任何一处单独改动都会让引擎与客户端对不上。动它们之前先读 `docs/ARCHITECTURE.md` 的「文本协议契约」一节（开局指令四种变体、分项美术指令、章节协议、**行动** 格式、【图】标记规则、音频三行、段过滤原理），以及「逐轮状态快照与精确回退」「音频管线」两节。

| 你改的字符串 | 同步点 |
|---|---|
| 开局指令（`开局：《…》。主角卡：…` 等四种变体，含待命/跳过后缀） | `SKILL.md`「客户端开局指令」· `parser.ts` `buildCustomOpening`/`buildQuickOpening` · `tests/parser.test.ts` 字符串快照 |
| 分项美术指令（`美术：立绘 <名>` / `美术：背景 <名>` / `开演。` / 待命后缀） | `SKILL.md`「分项美术指令」· `parser.ts` `buildArtCommand`/`BUILD_START` · `tests/parser.test.ts` 字符串快照 |
| 章节协议（`规划：第 N 章。` / `【清单】立绘\|<名>` / `【清单】背景\|<名>` / `【章】第 N 章 完`） | `SKILL.md`【章节规划指令】【章节与剧情树】· `parser.ts` `parseManifest`/`parseChapterMark`/`buildPlanCommand` · `tests/parser.test.ts` 与 `tests/crafting.test.ts` 快照 |
| `**行动**` 选项段 | `SKILL.md`「每轮协议」+ `RULES` 第 1 句（`protocol-lines.mjs`，入口 re-export）· `parser.ts` `parseOptions`/`stripOptionsBlock` |
| `【图】` 标记行 | `SKILL.md`「美术」「美术预载」+ `RULES` 第 3 句 · 类型集合真源 `shared/protocol.mjs` 的 `ART_KINDS`/`ASSET_KINDS`（`ART_LINE_BODY` 与资产文件名 `ASSET_FILE_RE` 都由它构造，server/scripts 不许再手写交替组——契约 lint ⑤ 组钉住）· `parser.ts` `scanMarkers`/`finalMarkers` · `acp-server.mjs` `handleArtLine` 正则、`persistAsset` 落盘名与目标目录（`presets/<剧本 id>/assets/`，封面 `presets/<id>/cover.jpg`） |
| 差分与表情切换（`【清单】立绘\|<角色>-<变体>` / `美术：立绘 <角色>-<变体>` / `【立绘】<角色>\|<变体>`） | `SKILL.md`【章节与剧情树】【每轮协议】【美术】差分规则与导演层第 8 条 · `parser.ts` `splitAssetVariant` · `protocol-lines.mjs` `parseExpressionLine` → 入口广播 `expression` 事件 · `store/portrait.ts` `nextPortraitOnExpression` · `tests` 快照 |
| 音频三行（`【曲】<名>` / `【环境】<名>` / `【音效】<名>`，停止 `【曲】停`/`【环境】停`；文件名 `<类型>-<名>.<ext>`） | `SKILL.md`【音频】+ 导演层第 9 条 + 每轮协议第 2 条 + `RULES` 第 6 句（`protocol-lines.mjs` 逐字同一份）· `shared/protocol.mjs` 的 `PROTOCOL_HEADS`（9 项）/`AUDIO_KINDS`/`AUDIO_EXTS`/`AUDIO_FILE_RE`/`AUDIO_REL_RE`/`AUDIO_MIME`（唯一真源；`parser.ts` re-export 并构造 `isProtocolLine`、`acp-server.mjs` import 消费）· `protocol-lines.mjs` `parseAudioLine`、`audio.mjs` `scanPresetAudio`、`routes.mjs` 的 `GET /api/audio?preset=`/`GET /audio?p=`（白名单 + `path.resolve` 前缀 + MIME + 长缓存）、入口 `handleArtLine` 的 audio 分支 → `audio` 事件 · `lib/audio.ts` `AudioManager`、`lib/acp.ts` `fetchAudio`/`audioFileUrl`、`store/slices/gameplay.ts` 的 `audio` 分支 · `tests/parser.test.ts`（协议头断言）、`tests/server.test.ts`、`tests/integration/audio-history.test.ts` |
| 协议常量（`PROTOCOL_HEADS` 9 头 / `AUDIO_KINDS`·`AUDIO_EXTS` 等音频白名单与直服正则 / `ART_KINDS`·`ASSET_KINDS`·`ASSET_FILE_RE` 美术类型与资产文件名 / `DIRECTIVE_PREFIX_RE` 指令前缀 / `CHAPTER_MARK_RE` 章标记） | 真源 `shared/protocol.mjs`（唯一一份值；改这里即全链生效）· `shared/protocol.d.mts` 类型镜像（`PROTOCOL_HEADS`/`AUDIO_KINDS`/`ART_KINDS`/`ASSET_KINDS` 用字面元组，其余不重复字面）· `parser.ts` re-export（公共 API）与 `isProtocolLine`/`ART_LINE_BODY`/制作清单正则的构造 · `acp-server.mjs` import（`pickEffort`/`isMainTurn`/quality guard/`scanPresetAudio`/`/audio` 直服/导入包白名单）与 re-export（doctor 面）· `protocol-lines.mjs`/`audio.mjs`/`assets.mjs` 的构造与 re-export · `tests/contract.test.ts` ①⑤⑥组 · `docs/adr/0012` |
| 主题兜底色（`accent`/`accent2` 默认值 + font/dialog 白名单） | 三处字面同批改：`acp-server.mjs` `DEFAULT_THEME`/白名单（`/api/presets` 出口的兜底）· `src/theme.ts` `FALLBACK_THEME`/`FONT_PRESETS`/`DIALOG_TEXTURES`（客户端更严校验的回退）· `src/styles/global.css` 的 `--accent`/`--accent2` 初始变量（首帧/无注入态）· `tests/contract.test.ts` ⑥组逐键钉死三处一致（含 global.css 第三份字面）· `scripts/doctor.mjs` 的双层 theme 检查消费同源 |
| 重绘指令（`美术：重绘 <类型> <名>[-<变体>]` 与【图】第四段「重绘」） | `SKILL.md`【素材重绘】+ `RULES` 第 3 句 · `parser.ts` `buildRegenCommand` · `protocol-lines.mjs` `parseArtLine` regen 分支与入口 `persistAsset` 覆盖 · `tests` 快照 |
| 创作模式（`创作模式：进入剧本创作。` / `装配。` / `【新剧本】<id>`） | `SKILL.md`【剧本创作】+ `RULES` 第 3 句 · `parser.ts` `ENTER_CREATION`/`BUILD_ASSEMBLE`/`isProtocolLine` · `protocol-lines.mjs` `parsePresetAddedLine` → 入口 `handleArtLine` 广播 `presetAdded` 事件 · `tests` 快照 |
| 世界段与续玩指令（`世界：<worldId>。` / `继续世界：<worldId>。`） | `SKILL.md`【世界线】【启动流程】· `parser.ts` `buildCustomOpening`/`buildQuickOpening`（带 worldId 参数）/`buildResumeCommand` · `tests/parser.test.ts` 字符串快照 |
| 剧情编辑（`剧情：<指令>` / `【树】` 标记行） | `SKILL.md`【剧情编辑指令】· `parser.ts` `buildTreeEditCommand`/`isProtocolLine`（纳入【树】）· `protocol-lines.mjs` `parseTreeLine` → 入口 `handleArtLine` 广播 `treeEdited` 事件 · `store/slices/gameplay.ts` 编辑回合不进历史 · `tests` 快照 |
| 世界路径（`state/worlds/<worldId>/…`） | `SKILL.md` 全文 state 路径 · `worlds.mjs` `migrateLegacyState`/`listWorlds`/`forkWorld` 与索引 · `RULES`「世界纪律」句 |
| 删除进回收站（`state/trash/<ts>-<rand4>[-<剧本id>]-<原名>/`，EXDEV 回退直删标 `fallback:"purged"`） | `worlds.mjs` `moveToTrash`（唯一入口）+ `deleteWorld`（返回带 `trashed`）与 `routes.mjs` 的 `/api/assets` delete 分支（返回带 `trashed`）· `state/README.md`「回收站」手工恢复指引 · `docs/adr/0014` · 客户端去向文案：`WorldsScreen` 删除成功行内提示、`store/slices/assets.ts` 删除汇总 · `tests/server.test.ts`、`tests/integration/audio-history.test.ts`、`tests/e2e-ui/worlds.spec.ts` |
| 逐轮快照与精确回退（`state/worlds/<worldId>/history/NNNN.json`，条目 `{seq,at,kind:"turn"\|"backup",nodeId,chapterNo,files}`） | 落盘与语义：`snapshots.mjs` `writeSnapshot`/`readSnapshot`/`readSnapshots`/`selectSnapshotForNode`/`writeWorldFiles` 与 `worlds.mjs` `forkWorld`/`restoreWorld` + 正戏回合判定（入口 `sendPrompt` 内 `flushArtLines()` 之后、`busy=false` 之前；引擎回 JSON-RPC error response 时按失败回合传播——error 事件 + 409 + 不写快照）· 端点 `GET /api/history?worldId=[&seq=]`、`POST /api/worlds {action:"fork"\|"restore"}` · `docs/ARCHITECTURE.md`「逐轮状态快照与精确回退」· 客户端 `lib/acp.ts` `fetchHistory`/`fetchSnapshot`/`WorldSnapshot`、`store/slices/tree.ts` `restoreSnapshot`（回退成功后补发 `继续世界：` 让引擎重读档）、`StoryTreeScreen` 的存档点标注（「存档点 · 第 N 幕」）/原地回退/分叉带 seq、快照对比（v1.7：`prevSnapshotSeq` 基线含 backup + `lib/diff.ts` 行级 LCS + 三 tab 面板） · 回退后的客户端语义：history 非破坏式分割线（`types.ts` `HistoryRollbackMark` + `HistoryDrawer` 旧幕置灰）、`pendingResync`/`resyncFailed`（`gameplay.ts` turn_end 清除与 error 标记、`tree.ts` `retryResync`）、TopBar 徽章与「再同步」按钮、`WorldsScreen` 行内小标、treeNotice 三态 · `tests/server.test.ts`、`tests/integration/audio-history.test.ts`、`tests/diff.test.ts`、`tests/ui.test.tsx`、`tests/integration/pipeline.test.ts`（error response） |
| 世界线 label/note 与导出导入（`<worldId>.world.json`、`format:"bunkiten-world"` v1、label ≤60 / note ≤200） | `worlds.mjs` `updateWorld`/`exportWorld`/`importWorld`（重名加 `-2`/`-3` 后缀、`files.state` 必须非空）· 端点 `POST /api/worlds {action:"update"\|"import"}`、`GET /api/worlds/export?worldId=` · `index.json` 的 `label`/`note` 与 `listWorlds` 回填 · `lib/acp.ts` `WorldBundle`/`worldExportUrl`/`postWorldImport`/`postWorldUpdate`、`store/slices/world.ts` `parseWorldBundle`、`WorldsScreen` 行内改名/导出/导入（都收在行的 `⋯` 菜单里） · 分叉血缘只走 `forkedFrom`（+ `seq`）与 `fork.md`——`worlds.mjs` `forkWorld` 写进索引的 `note` 恒为空串，旧版自动写的那串由 `lib/worlds.ts` 的 `isLegacyForkNote` 判为「没有备注」 · `tests/server.test.ts`、`tests/ui.test.tsx`、`tests/integration/audio-history.test.ts` |
| 剧本导出包（`<id>.preset.json`、`format:"bunkiten-preset"` v1，`{id,title,exportedAt,presetMd,assets,audio}`，二进制 base64） | `presets.mjs` `buildPresetBundle`/`importPresetBundle`（重名加 `-2`/`-3` 后缀；文件名单层且不含 `..`、assets 限 jpe?g、audio 限 `AUDIO_EXTS`（从既有常量取集合，**不抄第二份**）、严格 base64（round-trip 拦静默丢字符）、解码总量 ≤50MB；落地 id 与 frontmatter id 不一致时改写 preset.md 的 `id:` 行）· 端点 `GET /api/presets/export?id=`（attachment 下载、不存在 404）、`POST /api/presets {action:"import",bundle}`（**body 上限 50MB 只给此端点**，`readBodyText` 可选 `maxBytes`，其余调用点仍 5MB）· `lib/acp.ts` `PresetBundle`/`presetExportUrl`/`postPresetImport`、`store/slices/nav.ts` `parsePresetBundle`/`importPresetText`（成功重取轮播 + `titleNotice`）、`TitleScreen` 当前卡「导出」按钮（`preset-export-<id>`）与「导入剧本」入口（`preset-import`/`preset-import-input`）· `docs/ARCHITECTURE.md`「剧本导出包」· `tests/server.test.ts`、`tests/integration/pipeline.test.ts`、`tests/ui.test.tsx`、`tests/e2e-ui/presets.spec.ts` |
| 设置键（localStorage `bunkiten.settings.v1`：master/bgm/ambient/sfx/muted/textSpeed/autoAdvance） | `lib/settings.ts`（`DEFAULT_SETTINGS`/`normalizeSettings`/`loadSettings`/`saveSettings`/`TEXT_SPEED_MS`）· `SettingsScreen.tsx`、游戏屏命令轨「设置」入口与 `DialogueBox` 面板内「自动」开关（同一份 autoAdvance 的两处入口）、`App.tsx` 启动时 `applySettings` 同步给音频管理器 · `lib/audio.ts` `applySettings`（主音量 × 通道、静音归 0）· `store/context.ts` 的自动前进倒计时（`armAutoAdvance`/`resumeAutoAdvance`/`cancelAutoAdvance`；`App.tsx` 捕获阶段的 pointerdown/keydown/input 是唯一一处「玩家接管」取消点） · `tests/ui.test.tsx` |
| 玩家侧文案（引擎状态串 → 玩家说法；裸 id、`快照 #N`、轮/回合都不上玩家的屏） | `lib/status.ts` `playerStatus`（映射表逐字翻译 + `出错：X` → `出错了：X`，表外状态原样透传——引擎新增状态串时在这里补玩家说法）· `lib/worlds.ts` `LEGACY_FORK_NOTE_RE`/`isLegacyForkNote`（旧版分叉备注按「没有备注」处理）· 消费方 `TopBar`/`CraftingScreen`/`App.tsx` `StatusAnnouncer`（aria-live）/`WorldsScreen`/`TitleScreen` · 术语与显示名回退链见 `CONTEXT.md` · `tests/ui.test.tsx` |
| 本地端点纪律（跨站 403 / body 5MB→413） | `http-util.mjs` `LOCAL_ORIGIN_RE`/跨站判定 + `MAX_BODY_BYTES`/`readBodyText` 统一入口（`/prompt`、`/api/worlds`、`/api/assets` 共用；先回 413 再断连；可选 `maxBytes` 唯一消费方是剧本导入 50MB）· `tests/integration/http-guard.test.ts` |
| 资产路径（`presets/<剧本 id>/assets/<类型>-<名>.jpg`；旧档 `assets/<文件>.jpg` 兼容） | `SKILL.md`【美术】生成前缓存检查与「资产目录纪律」+ `RULES` 第 3/4 句 · `assets.mjs` `assetRelPath`/`presetAssetsDir`/`presetIdFromPath`/`legacyAssetCandidates` 与入口 `sniffPreset`、`routes.mjs` 的 `/api/assets?preset=` 必填（否则 400）与 `/img` 白名单旧档直服 · `lib/acp.ts` `fetchAssets(preset)`/`assetFileUrl`/`presetQuery` · `tests/server.test.ts` 快照 |
| 段过滤（旁白兜底） | `acp-server.mjs`（seg 计数与事件）· `store/slices/gameplay.ts`（`seg`/`chunk`/`turn_end` 处理） |
| 回合日志与质量守卫（`logs/NNNN.json` 条目 `{seq,at,prompt,text}`；缺 `**行动**` 时自动补发一次「补充：」追问） | `SKILL.md`「每轮协议」第 5 条（引擎侧知情）· `protocol-lines.mjs` `SUPPLEMENT_PROMPT`（server→引擎内部指令，**不进 shared**）· `snapshots.mjs` `writeTurnLog`（seq 对齐取舍：快照去重/backup 会让两边步进错位，append-only 永不让步）· `acp-server.mjs` `supplementMissingOptions`（同一 busy 窗口、每回合一次、失败放弃留痕）与 `writeTurnSnapshot` 内的落盘点 · `worlds.mjs` `exportWorld`（刻意不含 logs/）· `tests/integration/pipeline.test.ts`（⑪–⑬）、`tests/server.test.ts`（writeTurnLog/SUPPLEMENT_PROMPT）· `docs/ARCHITECTURE.md`「回合日志与质量守卫」· `docs/adr/0015` |

## 门禁

- 交付前跑 `npm run build`（`tsc -b && vite build`）。
- 交付前跑 `npm run typecheck:server`（server/shared/scripts 的 checkJs 门禁：`tsconfig.server.json` 对 `server/**/*.mjs` + `shared/protocol.mjs` + `scripts/doctor.mjs` 开 strict 检查，类型全靠 JSDoc；CI 也会跑）。
- `npm test`（`vitest run --exclude "tests/e2e/**" --exclude "tests/e2e-ui/**"`）：parser 65 + server 110 + crafting 52 + treeLayout 7 + genealogy 8 + diff 8 + doctor 12 + preload 8 + ui 134 + integration 27（pipeline 15 + audio-history 9 + http-guard 3），**共 431 例**，秒级——默认含集成层，改契约字符串必须同步改快照；另跑契约 lint `tests/contract.test.ts`（v1.6 防漂移门禁，**不计入这 431**——它断言的就是上面这些数字与协议真源）。
- `npm run test:coverage`：**同一批测试文件**加覆盖率仪表（`@vitest/coverage-v8`，include 只圈 src/server/shared/scripts 四棵树；与 431/contract lint 计数无关，CASE_GROUPS 不动）。2026-09 基线：lines 75.77 / branches 65.87 / functions 78.56 / statements 73.62（server 树偏低是集成层在子进程里跑真 acp-server、coverage 追不到子进程，属已知口径）。thresholds = 基线四舍五入 - 2pp（lines 74 / branches 64 / functions 77 / statements 72），语义是**防下滑线**而非硬指标（实际余量约 2pp：1.56-1.87pp），主动提升覆盖率时同步上调。CI 以它替代 `npm test` 步骤（同一套测试避免双跑），并上传 HTML 报告 artifact。
- `npm run doctor`：剧本体检查（`scripts/doctor.mjs`，作者侧工具）——改 preset 结构/新增剧本后跑；退出码非 0 当且仅当有 error（warning 不影响）。**刻意不进 CI**：报告面向作者按需看，且「仓库此刻的 preset 健康度」随游玩数据（state/worlds、素材增删）漂移，门禁化会误伤；doctor 的纯函数判定已由 doctor 12 例守在 `npm test` 里。
- `npm run test:e2e`：真引擎冒烟（`tests/e2e/smoke.spec.ts` 两个 test，合计约 5 个真实引擎回合；本机实测 ~6–12 分钟，视模型与网络）。**前提：本机能出网到 x.ai**——需本机登录 grok CLI，代理环境要开 TUN 或给命令带 `https_proxy`（直连被墙时表现为回合 600s 超时）；改前端流程/协议后跑。
- `npm run test:e2e:ui`：假引擎确定性 UI e2e（`tests/e2e-ui/`，默认 chromium、不重试，秒级），改前端流程/协议/harness 后跑；CI 也会跑（先 `npx playwright install chromium --with-deps`）。
- 打包预检：`npm run dist:win` / `npm run dist:mac`，产物在 `release/`；`npm run dist:mac:dir` 只出 `.app` 目录（**不含 `app-update.yml`**，electron-updater 会静默降级）。
- 发版：打 `v<package.json version>` tag 即触发 `.github/workflows/release.yml`（guard 先校验 tag 与 `package.json` version 一致、并跑 `npm test`，两个平台 job 都 `needs: guard`）；本地 `dist:*` 只作预检，正式产物由 CI 双平台矩阵出。签名/公证条件化：`CSC_LINK` / `APPLE_*` 缺失时显式走未签名路径，构建照常成功。
- CI（`.github/workflows/ci.yml`）在 push/PR 上跑 `npm ci` + `npm run build` + `npm run typecheck:server` + `npm run test:coverage`（即上述 431 例 + 契约 lint + 覆盖率阈值，**替代** `npm test` 步骤避免双跑，另传 coverage 报告 artifact）+ 假引擎 UI e2e（`test:e2e:ui`，CI 内自装 chromium）；真引擎 e2e 需要本机登录 grok CLI、且能出网到 x.ai，只在开发机跑。

## 深入材料

- 动 ACP 会话、`/img` 与 `/audio` 解析顺序、资产/音频管线、世界线状态布局、逐轮快照与精确回退、世界线导出包、打包布局与已知限制 → `docs/ARCHITECTURE.md` 对应章节
- 动引擎叙事行为（导演层、音频纪律、状态纪律、尺度）→ `.grok/skills/bunkiten/SKILL.md`
- 想知道某个设计被否决的备选方案 → `docs/adr/0001-0017`；术语口径 → `CONTEXT.md`
- 给玩家的安装说明 → `QUICKSTART.md`

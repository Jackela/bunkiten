# 快速上手

写给你——拿到安装包的那位朋友。五分钟能进游戏，遇到问题看最后一节。

## Windows 安装

1. 拿到 `release/` 目录里的安装包：**Bunkiten Setup.exe**（安装版）或 portable 单文件版（免安装，双击即玩）。
2. 安装版一路下一步；装完桌面/开始菜单里出现「Bunkiten」。
3. 第一次启动，如果画面提示**还没有连上叙事引擎**（新电脑需要一次性准备），有两条路：
   - **用自己的密钥**（不装终端）：点「填自备密钥」，按下面「用自己的密钥」一节填服务地址与密钥即可；
   - **装 CLI 并登录**：
   1. 打开 PowerShell（开始菜单搜 PowerShell），粘贴运行：
      ```powershell
      irm https://x.ai/cli/install.ps1 | iex
      ```
      这会安装 grok CLI（游戏的 AI 引擎）。
   2. 装完后**新开**一个终端窗口，运行 `grok login`，按提示在浏览器里登录你的 xAI 账号。
   3. 回到游戏，点「重试」。

## macOS 安装

1. 打开 dmg，把 Bunkiten 拖进 Applications。
2. **第一次打开**：因为安装包没有签名，系统会拦。在「应用程序」里找到 Bunkiten，**右键 → 打开** → 再点「打开」。只需要做这一次。
3. 若提示未连上引擎，两条路：点「填自备密钥」用自己的服务（见下节），或先装好 grok CLI（xAI 官方命令行，安装指引见 x.ai/cli），装好后打开「终端 Terminal」运行 `grok login` 登录，再回游戏点「重试」。

## 用自己的密钥（不装终端、不登录）

引擎默认用你终端里的 grok 登录态；也可以用**自己的模型服务**跑（任何说 OpenAI 兼容协议的服务：OpenAI、DeepSeek、通义、智谱、OpenRouter、Grok、本机 Ollama…）。两条入场任选一条，随时可以互换。

**怎么填**：启动屏点「填自备密钥」，或进游戏后右上角**设置 → 引擎与密钥**。

- **故事引擎**：切到「自备密钥」→ 选一个服务（预置了常见服务：OpenAI、Gemini、Mistral、OpenRouter、DeepSeek、Kimi、智谱、百炼、SiliconFlow、火山方舟、Groq、Together、Fireworks、Grok、本机 Ollama/LM Studio，以及国内的千帆、混元、星火、MiniMax；同一个服务有国内站与海外站的分列两条，**按你买服务的那个站选**，密钥与站点不配套会报 401）→ 填服务地址、密钥、模型名。「测试连接」会真的连一次，告诉你通不通、花了多少毫秒；不通会给一句原因（原因里不会出现你的密钥）。这份服务商列表会**在线更新**：联网时自动取最新的一份，离线时自动用随包内置的列表，照样能选。
- **插画生成**：想让立绘/背景由你自己的图片服务画，就切到「自备服务」并填同一组信息（地址 / 密钥 / 出图模型），尺寸两格通常留空即可（立绘与封面竖构图、背景横构图自动选；想固定就填「出图尺寸」，只想改背景就填「背景尺寸」）。不填也行——不配就是不出图，剧情照常进行。
- **「测试连接」会真的花一点点钱**：对话侧只拉一次模型清单（免费）；**图片侧会真的生成一张小图**来验证服务能用——这是唯一能证明「它真的能出图」的办法（有些服务列表里有模型但出图是被限额/未开通的）。不想花就别点，直接填完保存即可。

**怎么生效**：改动即时保存，但引擎要**重启一次**才读得到新配置——填完点同一节里的「立刻重启引擎」（正在演绎时按钮会被拒绝，等这一回合结束再点）。

**密钥存在哪**：只存在这台电脑上的 `~/.bunkiten/credentials.json`（文件权限 0600）。它**不进剧本、不进存档、不进任何导出包**；界面上只显示掩码（像 `sk-…4f2a`），完整密钥不会从服务端回到屏幕上。

**怎么撤销**：点密钥格右边的垃圾桶图标清空，或把模式切回「沿用终端登录」——立刻回落到 grok 登录态，没有残留。

**失败了会怎样**：对话侧连不上会有明确报错（「测试连接」先告诉你原因）；出图失败的图会被**安静地略过**——正文与选项绝不提图片，游戏照常推进，你只会发现那张图没出现。

## 怎么玩

1. **选剧本**：标题屏像老游戏机换卡带——用 ← → 键（或点屏幕左右两侧、左右拖拽）切换卡带，按 Enter（或点中间那张）把它插进卡槽。每张卡带都印着剧本自己的封面图（没有封面时显示该剧本主题色的渐变）。
2. **选世界线**：插卡后进入「世界线屏」——一条世界线就是一局独立存档，多周目、平行线互不干扰。用 ↑ ↓ 选中（或鼠标悬停），点「继续」读档续演（Enter 也行）；点「新世界线」从捏主角开始一局全新的；每条世界线都能「编辑」（显示名 + 备注）、「导出」（下载成 `.world.json`）、「删除」（先点「删除」再点「确认删除」，两段确认；删掉的东西挪进游戏数据目录的回收站 `state/trash/`，不自动清理，误删可按 `state/README.md` 手工找回），顶栏还有「导入」把别人给你的世界线读回来；列表↔**家谱**一键切换——家谱把「哪条线从哪条线的哪个节点分叉」画成一张血缘森林图，方向键在图上走、Enter 选中——细节见下面「世界线管理」一节。**分叉**是从某个已走过的节点另开一条世界线：它只复制当前进度与剧情树、把进度回退到那个节点，**不会自动推演**，切换过去后你从那儿接着玩。
3. **捏主角**：点「快速开局」直接用剧本预设主角开演；或者老老实实把每个问题选完（标注「选 1-2」的能选两个），每项都选好才能开演（继续已有世界线时跳过这步，直接续演）。
4. **制作美术（按章节）**：开演前二选一——「制作美术并开演」进入「制作中」屏：游戏按章节推进，先写第 1 章大纲，再按清单把本章全部角色立绘和场景背景逐张画好（包括你未必走到的分支；主要角色还会多画几张表情差分，演出时会随情绪切换），画完才开演，玩的过程零等待。首次开一个新剧本，每章等 8–20 分钟属正常（占位剪影会逐格点亮，后续章节会自动跳过已画好的立绘）；不想等就点「跳过剩余，立即开演」，缺的图边玩边补。「跳过美术，直接开演」不进制作屏，立刻开始剧情。
5. **玩**：看剧情（打字没打完时点一下对话框或按空格，整段立即显示；角色立绘会随剧情情绪切换表情差分），然后——
   - 点选项按钮（也可以直接按 **1-9** 选对应那条），或
   - 在下面输入框直接打字（想说什么就写什么），或
   - 在输入框里输入选项前面的编号（如 `1`），效果等同于点那条选项。

   每一章打完会自动回到「制作中」屏制作下一章（大纲 → 本章美术），做完自动接着演下一章。

   界面左上角平时不占画面——只有引擎忙、出错或「待重同步」时才亮出状态簇；章号改由每章开头的**过场卡**（正中亮一次「第 N 章」）与「历史」抽屉的标题呈现。右侧竖排按钮轨从上到下是 **历史**（回想：前几幕回顾）、**角色**（面板：好感度/表情/秘密（默认折叠防剧透）/幕后手记/线索·未了伏笔，开着的话每回合后自动刷新）、**画廊**（素材）、**剧情图**、**重演**（对刚走完的这一幕不满意就重演：撤销它并自动重发你刚才那句输入，重新演绎一遍；本世界第一幕不能重演，可以连着来）、**重开**（回标题屏重新选）、**前情**（让引擎复述剧情）、**换剧本**、**设置**（齿轮，调音量与静音、文本速度、自动前进）、**帮助**。

   **Esc 键**从里往外依次关掉当前打开的东西：画廊大图 → 剧情图里的节点详情 → 历史抽屉 → 角色面板 → 创作屏的退出确认 → 剧情图屏 → 世界线屏（回到标题屏）。输入框里正在打字时 Esc 不会拦截（先点一下空白处再按）。

6. **剧情图（看剧情树 / 一句话改树 / 在此分叉 / 回退 / 存档点对比）**：点右侧轨的「剧情图」打开——顶部章节切换器列出解析出的每一章（带进度指针的那章标「当前」，点谁画谁；归档章只剩目录信息），每章一张节点图（金=已走过、暗虚线=已剪枝、紫=即兴嫁接、亮环=当前进度），点节点看详情（地点/在场/梗概/出边）；在已走过的节点上可以「在此分叉」，也可以「回退到此节点」精确回到那一刻（会覆盖当前这条世界线的进度并让引擎重新读档，详见下面「剧情图里的回退」一节）；有基线的节点还能「**与上一个存档点对比**」——当前状态/前情提要/剧情图三个 tab 逐行 diff，一眼看出这一幕改了什么（+绿 −红，没变的行折叠）。底部输入框能**一句话改树**（例：「加一个雨夜遇袭的场景」），引擎改完剧情图会自动刷新——引擎正忙时指令会排队，就绪后自动发送。本章节点太多（超过 40 个）时会默认切成列表模式，右上角能切回图形；图形模式滚轮缩放、拖拽平移、双击复位。

7. **标题屏的进阶入口**：
    - **素材**：打开画廊，翻看**当前剧本**已生成的立绘（含表情差分，按角色分组）、背景和封面——素材跟着剧本走，换剧本就换一批；点任意一张看大图，不满意就点「重新生成」重画这一张（引擎正忙时按钮会暂时禁用，稍等再点）。素材多的时候点「**管理素材**」：勾选多张后可以**批量重绘**（一条画完再派下一条，别催）或**批量删除**（两段确认；封面不参与批量删除）。
   - **创作新剧本**：进入创作模式，像聊天一样描述你想要的故事（题材、角色、基调想到哪说到哪），聊到满意点「开始装配」，引擎自动写出剧本文件、封面和主要角色立绘；完成后点「去选它」——新卡带已经出现在标题屏轮播里，直接开玩。
   - **导出 / 导入剧本（分享用）**：当前卡带右上角「**导出**」把整个剧本（preset.md、全部立绘/背景/封面、音频）打包成一个 `<剧本 id>.preset.json` 单文件，发给别人即可；对方点右下角「**导入剧本**」选这个文件，剧本立刻进轮播（重名会自动改成 `<id>-2`，绝不覆盖已有剧本，单包上限 50MB）。

进度自动保存在游戏目录里，关掉再开会在原地续演。

## 设置（音量、文本速度、自动前进）

右上角按钮轨最下面那个**齿轮**就是设置，玩的时候随时能开，改了立刻生效。这些设置存在这台电脑上，不写进存档，换世界线也不会重置。

- **主音量**：总闸，拉小了三路声音一起小。
- **静音**：一键全静。它不会改你各通道自己的音量值，取消静音就回到原来的大小。
- **曲（BGM）/ 环境 / 音效**：三路各自调。BGM 是配乐，环境是雨声、炉火这类一直响着的底噪（这两路切换时约有半秒淡入淡出，不会硬切），音效是门响、脚步这类响一次就完的动静。
- **文本速度**：慢 / 标准 / 快 / 瞬间四档。前三档决定打字机一个字停多久（40 / 24 / 12 毫秒一字），「瞬间」就是正文一下全出来（跟每次手动补全一个效果）。
- **自动前进**：关 / 3 秒 / 5 秒。正文打完、选项也出好之后开始倒数，到点自动选**第一条选项**往下走（看长对话不用一直点）；倒计时就显示在选项上方（「自动前进 · 3s」），你一旦自己动手（点选项、打字）这一轮就不自动走了。
- **引擎与密钥**（v1.10）：换用你自己的模型服务与出图服务——怎么填、存哪、怎么撤销见上面「用自己的密钥」一节。

想立刻跳过打字机：按**空格**，或点一下对话框。

另有一样不在设置里的开关：**系统的「减少动态效果」**（macOS「辅助功能 → 显示 → 减少动态效果」、Windows「辅助功能 → 视觉效果」）。系统开了它，游戏就自动跟随——位移类动效（颗粒漂移、光斑上浮、卡带插入等）变瞬时、脉冲光标静止、打字机直接整段显示；淡入淡出保留（这是推荐的无障碍替代，不是位移）。它跟系统走，游戏里没有也不需要单独的开关（系统里已经选过一次了）。

## 键盘操作

鼠标能做的键盘基本都能做：

- 标题屏：← → 换卡带，Enter 装载。
- 世界线屏：↑ ↓ 选，Enter 继续（家谱视图里方向键在血缘图上走位，Enter 选中）。
- **1-9**（主键盘和小键盘都行）：直接选中对应的选项按钮，不用点鼠标。正在输入框里打字时数字就是数字，不会抢你的字。
- **空格**：打字机还没打完时，补全整段正文。
- **Esc**：从里往外关掉当前打开的东西（画廊大图 → 剧情图节点详情 → 历史抽屉 → 角色面板 → 创作屏退出确认 → 剧情图屏 → 世界线屏回标题屏）。
- 剧情图：滚轮缩放、拖拽平移、双击复位（`+` / `-` / `0` 也行）、方向键在节点间走、Enter 看详情。

## 世界线管理（重命名、备注、导出、导入）

世界线就是一条独立存档，多周目、平行线互不干扰。除了「继续」，每条世界线上还有几个小按钮：

- **编辑**：给这条世界线起个你认得出的**显示名**（最多 60 字），再写一句**备注**（最多 200 字，比如「这条线走了教堂结局」）。备注会以小字显示在名字下面，方便多条线摆在一起时认人；两个都留空 = 清掉，列表就回到显示世界线 id。Enter 保存、Esc 取消。
- **导出**：把这条世界线打包下载成一个 `<世界线 id>.world.json` 文件——里面有当前进度和**全部逐轮快照**，用来备份、换电脑、或者发给朋友都行。
- **导入**：选一个 `.world.json` 读回来。id 撞车不会覆盖你已有的存档，会自动排到 `<id>-2`、`-3`，并给新世界线标上「（导入）」的备注。
- **删除**：老规矩，两段确认（点「删除」→ 点「确认删除」）。删掉的世界线不是凭空消失——它被整体挪进游戏数据目录的回收站 `state/trash/`（不自动清理、不占游戏界面）；真删错了，按 `state/README.md` 里的说明手工挪回来即可。
- **家谱视图**：列表左上有「列表 / 家谱」切换。家谱把所有世界线的分叉血缘画成一张森林图（谁从哪条线的哪个节点分出来，连线一目了然）；标 ⌫ 的是「父线已删」的孤儿线；方向键在图上走位（←→ 同层、↑↓ 跨层），Enter 选中后快捷条能直接「继续」，或点「查看」跳回列表对应行。

## 剧情图里的「回退」（回到某一幕）

游戏每走完一幕正文，都会在存档目录里留一份**逐轮快照**（`state/worlds/<世界线>/history/` 里那些 `NNNN.json`）。所以剧情图上的节点会带一行 `存档点 · 第 12 幕` 这样的标注，告诉你哪一步能精确回去：

- 点一个有快照的节点，详情里有「**回退到此节点**」。两步确认后，游戏会把这条世界线的进度**覆盖**成那一刻的样子，然后让引擎重新读档、恢复画面（该有的立绘背景会跟着回来），接着往下演——**不是**从头重开，也**不是**另开一条线。
- 回退前，你现在的进度会**自动备份**成一条快照（提示里会写备份编号），万一退多了还能退回来。
- 引擎还在写这一幕时不能回退，会提示「忙碌中，等这一幕结束再回退」，等它写完再点。
- 你要是想**保留**现在这条线、又从某个节点另开一条，就用节点详情里的「**在此分叉**」：有快照时是精确分叉（连那一刻的进度一起复制过去），没有快照的老存档按老办法只回退进度指针。
- 点开节点详情还能「**与上一个存档点对比**」：拿这一刻的存档和上一份快照逐行比（当前状态 / 前情提要 / 剧情图三个 tab，+绿 −红），这一幕引擎到底改了什么一目了然。回退前的自动备份也算合法的对比基线，所以回退后的第一次对比也有得比。

大剧情图看着累时它自己会帮你：本章节点超过 40 个就默认变成**列表**（一行一个节点，地点/梗概看得清），右上角能切回图形；图形模式支持滚轮缩放、拖拽平移、双击复位，左下角显示当前缩放比例。

## 重演这一幕（对刚才那幕不满意）

骰子掷得不好？右侧按钮轨的「**重演**」就是为这准备的：它撤销**刚走完的这一幕**（画面、进度、存档都退回去），然后把你刚才那句输入**原样再发一遍**，让引擎重新演绎。几个要点：

- 历史里不会留下两条正文，只插一条分割线（「—— 第 N 幕已重演 ——」），旧幕置灰但不删。
- 重演要先让引擎重新读档（不然它还记着上一版剧情），重读期间顶栏有「待重同步」徽章；万一重读失败，点旁边的「再同步」重试即可，重演会在成功后自动跟上。
- 本世界的**第一幕**没有可退的上一幕，不能重演；重演出的新一幕结束就能再来一次（可以连着重演）。
- 换世界线 / 换剧本 / 重开时，排队中的重演自动作废。

## 自动更新与未签名包

- 从官方 [Releases](https://github.com/Jackela/bunkiten/releases) 下载的**已签名**安装包会在启动时自己查一次新版本，有新版本会提示你，跟着点就行。
- **没签名的包不会自动更新**：macOS 的系统机制要求更新前后的签名一致，未签名的包装上去了就没法自己升级。所以拿到未签名包时，更新方式就是再去 Releases 下一个新的，覆盖安装即可——存档在游戏目录里，覆盖安装不会把它清掉。
- macOS 第一次打开未签名包，仍然要**右键 → 打开**（只需一次）。

## 剧本更新

拿到的是 `.preset.json` 导出包？更简单——标题屏右下角「**导入剧本**」选文件就行，下面这些步骤都不用。拿到的是剧本文件夹（里面有 `preset.md` 的那种）才需要手动放：

- **Windows**：放进游戏安装目录下的 `resources\game\presets\`（安装版默认在 `C:\Users\<你>\AppData\Local\Programs\Bunkiten\resources\game\presets`），重启应用。
- **mac**：右键应用 → 显示包内容，放进 `Contents/Resources/game/presets/`，重启应用。

如果那个剧本文件夹里还有 `assets\` 子目录或 `audio\` 子目录，一起拷过去——立绘、背景和声音都跟着剧本走，放在剧本自己的文件夹里。

## 给作者：给剧本加音频（可选）

想让你的剧本有声音，就在剧本文件夹里再建一个 `audio/` 子目录，往里放音频文件，命名规则是 `<类型>-<名>.<扩展名>`：

- 类型三种：`曲`（BGM，一直放的配乐）、`环境`（环境音，雨声、炉火这类底噪）、`音效`（门响、脚步这类响一次就完的动静）。
- 扩展名：`.mp3` / `.ogg` / `.m4a` / `.wav` / `.flac`。
- 例子：`presets/rift-mark/audio/曲-雨夜.mp3`、`环境-旅店大堂.mp3`、`音效-门响.wav`。

放进去就够了，不用改代码、不用改 `preset.md`：引擎在场景切换或情绪转折时会发【曲】/【环境】/【音效】行点名播放，名字必须和文件名里的 `<名>` 一模一样（短名，别带 `|` 和换行）。**没放文件、名字对不上、整个 `audio/` 目录不存在，都只是静默不播**——不报错、也不打断剧情，玩家最多觉得这剧本没配音。另外：引擎**不会**生成音频，也不会在你的标记里写路径，所有声音都得你自己放。

## 故障排查

| 症状 | 处理 |
|---|---|
| 一直卡在「连接中…」或提示连不上叙事服务 | grok CLI 不在 PATH：重装一次 CLI（上面那条 PowerShell 命令），或重启应用再试 |
| 提示未登录 | 终端运行 `grok login`，完成后回应用点「重试」 |
| 顶栏显示「出错了：timeout session/prompt」 | 引擎卡了一幕：等一会儿重发；频繁出现就重启应用 |
| 没有图片 / 出图失败 | AI 出图走账号套餐（Imagine 额度），限额时游戏照常能玩，只是少了图 |
| 听不到音乐或音效 | 先看设置里是不是静音、或哪路音量拉到 0；再确认剧本自己的 `audio/` 目录里放了这个文件（引擎点了名却没文件时是静默跳过，不报错） |
| 语音输入按钮不见了 | 正常：桌面版暂不支持语音，打字即可 |

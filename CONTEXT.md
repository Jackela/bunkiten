# bunkiten（分岐点）

LLM 驱动的单人互动 AVG（文字冒险 / 视觉小说）产品：引擎是 prompt（skill），剧本是数据，进度是文件。本表只收本语境特有的术语。

**两个层面分开读**：本表同时写着**内部/协议**用词（文件、端点、代码标识符：`history/NNNN.json`、`snapshot`、`turn`…）与**玩家可见**用词（屏幕上的字）。玩家的屏上不出现引擎腔与裸标识符——同一件事有两套说法的，条目里都点明（如「快照／存档点」「回合·轮／幕」）；改名只动呈现层，文件与端点名一律不变。

## Language

### 叙事结构

**章节（Chapter）**:
叙事与制作的最大单元，一局由 3-5 章组成；每章开演前必须完成该章的规划与全部美术制作。
_Avoid_: 关卡、幕（幕是玩家侧的一回正文，见「幕」）

**幕（Act）**:
玩家侧的叙事单位：一个正戏回合 = 一幕，历史抽屉与剧情图都按它编号（「第 N 幕」）。幕号统一取**逐轮快照序号**（第 N 幕 ⟺ `history/NNNN.json` 的 seq N，见「存档点」），所以它确定、可回溯，不是一个模糊序数；章节过场卡与回想抽屉标题上的「第 N 章」是章，不是幕。引擎与代码内部仍说「回合/轮」（`turn`、`turnSnapshots`、`rerollTurn`）——这两个词不上玩家的屏。
_Avoid_: 章（见「章节」）、回合 / 轮（内部术语）

**剧情树（Story Tree）**:
一章的完整分支结构，存于当前世界线的 `state/worlds/<worldId>/story-tree.md`，是导演层的骨架工具（玩家经剧情图查看）。
_Avoid_: 流程图、大纲（大纲是剧情树的一个字段）

**节点（Node）**:
剧情树上的一拍：一个地点、一组在场角色、一段梗概和若干出边。
_Avoid_: 场景（场景指地点画面）、拍点（旧词，已被节点取代）

**边（Edge）**:
节点间的可选走向，玩家选项是边的第一人称化身；自由输入不是边。
_Avoid_: 选项（选项是边在 UI 上的呈现）

**汇合点（Merge Point）**:
多条边重新汇聚的节点，用于控制树规模。
_Avoid_: 合并、收束

**嫁接（Graft）**:
玩家自由输入偏离当前节点时，把即兴结果作为新节点/新边接入树的动作（树内即兴支线，区别于世界线层的分叉）。
_Avoid_: 扩展、即兴支线

**剪枝（Prune）**:
玩家走过某条边后，把不再可达的枝标记为已剪枝（标记而非删除）。
_Avoid_: 删除

**终章节点（Finale Node）**:
每章唯一的收束节点，演出完输出章标记。
_Avoid_: BOSS 节点、结局（结局指整局的收束）

### 世界线与剧情图

**世界线（World Line）**:
一局独立存档：`state/worlds/<worldId>/` 下的三份文件（state.md / summary.md / story-tree.md），同一剧本可并行多局、互不干扰；`state/worlds/index.json` 是索引，旧扁平布局首次启动由 server 迁入 `main`。索引条目带展示名 `label`（≤60）与备注 `note`（≤200），整条世界线可导出为世界线导出包、也可导入回来（重名自动加后缀）。**显示名回退链**：`label` → `note` → 所属剧本标题 → 「未命名世界线」；**裸 `worldId` 永不上玩家的屏**，旧版 server 自动写进 `note` 的分叉备注（「分叉自 <id> @ <节点>」）按「没有备注」处理（见「分叉」）。导出文件名仍用 `worldId`——那是文件名，不是界面上的一句话。
_Avoid_: 存档槽、进度（进度指一条世界线内的当前节点指针）

**分叉（Fork）**:
从某条世界线的一个已走过节点另开一条世界线，**不推演任何内容**。有快照时走精确路径：以该节点最早匹配（或显式指定 seq）的快照三文件建新世界，索引记 `forkedFrom: { worldId, nodeId, seq }`；无快照时走兼容路径：复制当前三份文件、把进度指针回退到该节点（轮次清零、已剪枝恢复可达），由引擎在下次 `继续世界：` 时按 `fork.md` 一次性静默回退 state/summary 再删除它。两条路径都仍写 `fork.md`——精确路径下它只用来让引擎校准树。**血缘只走 `forkedFrom` + `fork.md`，索引 `note` 保持空串**：从前 server 往 `note` 里写「分叉自 <id> @ <节点>」，而 `note` 会被当成世界名显示，等于把裸 id 摊给玩家；玩家侧只看见家谱里的连线与「自《父线名》延伸」这类说明。
_Avoid_: 复制存档、回溯（回溯指回退进度本身）

**家谱（Genealogy）**:
世界线屏的血缘视图：把 index.json 里的 `forkedFrom` 画成森林（无来源的世界线是根，多根并列），一眼看出哪条线从哪条线的哪个节点分出来；父线已删的孤儿按根处理并带徽章标注。画布可滚轮缩放（指针为锚点）/ 拖拽平移 / 双击复位，工具条上的 `−` `适应` `+` 与键盘 `+ - 0` 是同一套动作；渲染宽度只封上界（单节点 ≈260–280px，两三条线的小森林不再被等比撑成巨卡），森林更大时照旧铺满可用宽度。
_Avoid_: 族谱、树（树指剧情树）

**快照（Snapshot）**:
每个正戏回合自动落一条的三文件全文存档：`state/worlds/<worldId>/history/NNNN.json`（4 位递增、append-only），条目为 `{seq, at, kind, nodeId, chapterNo, files:{state,summary,tree}}`，`kind` 是 `turn`（正戏回合）或 `backup`（回退前的自动备份）；与上一条逐字全等则跳过，`seq > 9999` 不再写入。规划/美术/剧情编辑/创作装配/待命回合不产生快照。
_Avoid_: 备份（玩家侧称「存档点」，见该条；文件与端点仍叫 `history` / `snapshot`）

**存档点（Save Point）**:
快照在**玩家侧**的称呼：剧情图节点标「存档点 · 第 N 幕」、对比入口写「与上一个存档点对比（第 N 幕 → 第 M 幕）」。幕号就是快照序号（第 N 幕 ⟺ seq N），所以「回到第 N 幕」在磁盘上就是那一份 `history/NNNN.json`。它是**呈现层用词**：文件、端点、代码标识符一律仍叫快照/`snapshot`。
_Avoid_: 存档（指整个世界线的进度文件，见「世界线」）、自动保存

**回合日志（Turn Log）**:
每个正戏回合「发了什么 + 引擎回了什么」的原文存档 `state/worlds/<worldId>/logs/NNNN.json`（append-only、不清理），与快照同一时机写入、只写不读；不进世界线导出包，是本机排障面。
_Avoid_: 聊天记录、对话历史（玩家侧那份叫「回想」，见该条；日志是排障面，不面向玩家）

**精确回退（Exact Restore）**:
从某条快照把世界线三份文件**逐字**恢复的操作：原地回退先写一条 `kind:"backup"` 的当前状态、再覆盖为目标快照（写备份失败即回退失败），成功后客户端补发 `继续世界：<worldId>。` 让引擎重新读档同步——磁盘上的三文件只是「档」，会话里留着回退点之后的记忆，必须让它重新读。分叉与回退都优先走快照；无快照才落回 `fork.md` 兼容路径，此时回退精度取决于引擎。玩家侧按幕说话（提示「正在回到第 N 幕…」、回退前提示「回退前的进度已备份」），回想抽屉里插一条非破坏式分割线「—— 已回溯到第 N 幕 ——」（重演那条写「—— 第 N 幕已重演 ——」）；动作名仍是**回退**，别拿「回溯」当术语（它只是那句分割线的口语）。
_Avoid_: 回溯、时光机

**重掷（Reroll）**:
撤销刚结束的正戏回合并重发同一玩家输入的操作：回退到次新的 turn 快照、让引擎重读档后自动重发原输入重新演绎；本世界第一回合不可重掷，可连掷。**玩家侧叫「重演」**（命令轨按钮、提示「正在重演这一幕…」「重演失败」），代码与文档内部仍是 `reroll`/`rerollTurn`。
_Avoid_: 重新生成、Retry（都不是重绘——重绘是美术资产操作）

**回想（Recall）**:
游戏屏右侧轨「历史」打开的那份前情回顾抽屉：最新一幕在最上，带当前章号（顶栏不显示章号）。非破坏式——被回退/重演覆盖过的旧幕只置灰，一条不删。玩家侧不再叫「回合记录」。
_Avoid_: 聊天记录、日志（日志是 `logs/NNNN.json` 排障面）

**待重同步（Pending Resync）**:
回退/重掷之后「引擎尚未重新读档」的会话内标记：磁盘三文件已覆盖、引擎会话记忆还停在旧时间线；补发的续玩指令成功收尾即清除，失败可重试。不持久化，重启后由「继续世界线」自然消化。
_Avoid_: 存档损坏警告（档没坏，只是引擎需要重读）

**世界线导出包（World Bundle）**:
一整条世界线的可迁移单文件 `<worldId>.world.json`（`format:"bunkiten-world"`、`version:1`）：三份文件全文 + 全部快照 + `label`/`note` + 元数据。导入时校验 format/version/worldId（`files.state` 必须非空），重名自动加 `-2`/`-3` 后缀，绝不覆盖既有世界，`note` 追加「（导入）」。
_Avoid_: 存档文件、迁移包

**世界段（World Segment）**:
会话指令里的世界标识：开局是 `世界：<worldId>。`（位于主角卡/快速开局段之后、美术结尾后缀之前），续演是 `继续世界：<worldId>。`（读档续演，跳过初始化与开场卡）。
_Avoid_: 存档指令、加载段

**剧情图（Story Graph）**:
剧情树的可视化界面（story-tree.md 的视图）：只读节点图（已走过/可达/已剪枝/嫁接）+ 节点详情 + 在此分叉 + 一句话自然语言改树；每个节点标注其最早匹配快照（「存档点 · 第 N 幕」）并可就地精确回退，当前章节点超过 40 个时默认降级为列表模式（可切回图形），图形模式支持滚轮锚点缩放与拖拽平移；解析失败时回退显示原文。**顶部章节切换器**列出解析出的每一章（带进度指针的那章标「当前」），点谁画谁——每一章都到得了，不再只看进度章；`归档` 章只剩目录信息（节点数据已不在文件里），点它给一句「这一章已归档，只保留了目录信息」而不装作能打开。≥lg 时节点详情住在右侧栏（sticky 自滚），窄屏回到画布下方。
_Avoid_: 流程图、思维导图、剧情树（剧情树是数据，剧情图是它的视图）

**剧情编辑（Story Edit）**:
剧情图屏发来的 `剧情：<自然语言指令>`，引擎静默增删改当前世界线的 story-tree.md 后回一句摘要，不改 state.md 与进度指针。
_Avoid_: 改树命令、树指令

**树标记（Tree Mark）**:
引擎改树后单独成段的 `【树】` 协议行，客户端据此刷新剧情图，不进正文与历史。
_Avoid_: 刷新标记

**回收站（Trash）**:
删除世界线与画廊素材的统一去处 `state/trash/`：删除不直删，整体挪进 `<时间戳>-<随机>-<原名>/`，误删可按 `state/README.md` 手工找回；不自动清理，没有恢复 UI。
_Avoid_: 软删除、undo（不是 UI 功能，是文件层面的后悔药）

### 对局界面

**角色面板（Characters Panel）**:
游戏屏的只读侧栏：解析当前世界 state.md 展示好感度、表情徽章、秘密（默认折叠防剧透）、幕后手记、线索与未了伏笔；面板开着时回合结束自动刷新。**玩家侧小节名**：state.md 的 `# 导演手记` 显示为「幕后手记」、`# Flags` 显示为「线索」、`# 未回收伏笔` 显示为「未了伏笔」（伏笔行尾的第 N 轮显示为「第 N 幕」）——**改名只动呈现层，state.md 的小节名与引擎写入口径不变**。
_Avoid_: 状态栏（状态栏指顶栏的状态点/状态文字）

**章节过场卡（Chapter Card）**:
章号一变就在画面正中亮一次「第 N 章」的过场（VN 惯例），约 2.2 秒后退场（最后 0.4 秒淡出）；不拦点击、不抢焦点，挂在 App 常驻层（跨屏记忆世界线与章号）——各 overlay 返回游戏屏时**不会**同一章再亮一次。游戏屏顶栏不再显示章号：章号现在只出现在这张卡与回想抽屉标题上（异常/进行中的状态簇仍留在左上角）。
_Avoid_: 章节提示条（顶栏旧形态）

### 制作管线

**制作清单（Manifest）**:
规划回合输出的一章美术需求清单（全部角色立绘 + 全部地点背景，含分支专属），前端据此构建制作队列。
_Avoid_: 资产列表、任务列表

**章标记（Chapter Mark）**:
终章节点演出完后的协议行 `【章】第 N 章 完`，触发下一章制作流程。
_Avoid_: 章节完成符

**制作中（Crafting）**:
章与章之间的等待屏：先规划（大纲）后美术，逐项点亮。
_Avoid_: 加载、预载（预载是 v1.1 旧词，指开局的制作中）

### 美术体系

**美术资产（Asset）**:
立绘/背景/封面的落盘文件，**随剧本走**：立绘与背景按剧本分组存放于 `presets/<剧本 id>/assets/<类型>-<名>.jpg`（差分即 `立绘-<角色>-<变体>.jpg`），封面同级 `presets/<id>/cover.jpg`；顶层全局 `assets/` 池自 v1.5.1 起废弃。一个 preset 文件夹因此自带全部素材，可整体分享或删除。音频素材是同级目录 `presets/<id>/audio/`，属另一条管线（不进 `assetRegistry`、`/api/assets` 不列）。
_Avoid_: 素材池、全局资产、图片库

**当前剧本 id（Current Preset Id）**:
本局 `state.md` 的 `- preset: <id>` 字段值，一切资产路径里的 `<剧本 id>` 都用它（与世界 id 无关）；state 尚未初始化时，用开局指令的 `开局：《<title>》。` 到 `presets/*/preset.md` 的 frontmatter `title` 反查目录名（自由输入开局用刚选定的剧本标题），创作模式装配期间用刚生成的 preset-id。
_Avoid_: 剧本名（title 是展示名）、卡带 id

**差分（Variant）**:
同一角色立绘的替代版本：表情差分（微笑/动容/伤感）或服装姿势变体（礼服/雨夜等），命名 `立绘-角色-变体.jpg`，无变体后缀即基础立绘。
_Avoid_: 皮肤、换装（换装只是变体的一种）

**表情切换（Expression Switch）**:
协议行 `【立绘】角色|变体`，由引擎按台词情绪驱动的画面切换指令；不是对话内容。
_Avoid_: 切立绘、表情标记

**封面（Cover）**:
剧本卡带的封面图，随 preset 目录分发（`presets/<id>/cover.jpg`），创作新剧本时自动生成。
_Avoid_: 卡面、海报

**重绘（Regenerate）**:
对单个已有素材按同源提示词重新生成并覆盖同名文件的管理操作，协议前缀 `美术：重绘`。
_Avoid_: 重画、刷新

**缓存权威（Cache Authority）**:
规划新章时以当前剧本的 `presets/<剧本 id>/assets/` 为唯一判据、清单只列新增项；客户端再做一层兜底归一化过滤（trim + 互为包含，且差分变体必须一致），背景与立绘一视同仁。
_Avoid_: 缓存优先、本地优先

**旧档兼容（Legacy Asset Fallback）**:
v1.5.1 之前的存档与【图】标记记的是全局 `assets/<类型>-<名>.jpg`；`/img` 收到这类路径时只在**当前剧本目录**内探测一次（命中直服、未命中 404），不跨剧本扫描、不迁落。
_Avoid_: 资产迁移脚本、双写目录

**创作模式（Creation Mode）**:
玩家用自然语言与引擎共创新剧本的流程：打磨对话 → 自动装配 preset 文件与封面 → 新剧本入轮播。
_Avoid_: 编辑器、剧本生成器

**画廊（Gallery）**:
素材管理界面：当前剧本的资产网格（`GET /api/assets?preset=<id>`，跨剧本不串味）+ 单项重绘；「管理素材」模式下可批量重绘（顺序队列）与批量删除（两段确认，封面不参与）。命令轨入口与屏名都叫「画廊」；角标只标异常（当前**未使用**的素材），「在用」是多数态、不标。组标题与角色名包在带底的面板条里（压在亮底图上也读得清），栅格按屏宽铺开。
_Avoid_: 素材库、图鉴、素材（指本功能时说「画廊」）

**剧本导出包（Preset Bundle）**:
一个剧本的可分享单文件 `<id>.preset.json`（`format:"bunkiten-preset"` v1）：preset.md 全文 + 全部立绘/背景/封面 + 音频，二进制一律 base64；与世界线导出包对称。导入重名自动加 `-2`/`-3` 后缀，绝不覆盖既有剧本。
_Avoid_: 剧本文件（指 preset.md）、存档包

### 音频

**音频（Audio）**:
剧本自带的演出素材，分曲（BGM，循环）/环境（环境音，循环）/音效（一次性）三类；作者手放 `presets/<剧本 id>/audio/`，命名 `<类型>-<名>.<扩展名>`（类型 ∈ 曲/环境/音效，扩展名 ∈ mp3/ogg/m4a/wav/flac）。引擎只用三条协议行切换：`【曲】<名>` / `【环境】<名>` / `【音效】<名>`，停止用 `【曲】停` / `【环境】停`（【音效】无停止语义）；三行与【立绘】同级、各自单独成段、不进正文与历史，**只报名字、绝不携带路径**。目录不存在或文件缺失一律静默（客户端只 `console.debug` 一行，剧情照常）——绝不生成音频、绝不落盘。
_Avoid_: 音乐生成、声音资源池

### 设置

**设置（Settings）**:
本机播放与阅读偏好：主音量 + 静音 + BGM/环境/音效三通道音量、文本速度（slow/standard/fast/instant）、自动前进（关 / 3 秒 / 5 秒）。持久化在 localStorage 键 `bunkiten.settings.v1`（非法值逐键回默认，版本内不迁移），是**跨世界线的本机偏好**：不写进 state、不进世界线、也不随导出包走。对话框面板右上角另有两个入口：「**自动**」是同一份自动前进设置的一键开关（打开落到 3 秒档；关掉会连已排好的倒计时一起取消），「**快进**」立即显示全文（打完即禁用）。玩家动手（点选项、打字）会取消本回合的自动前进，再点「自动」即恢复。设置屏里的「引擎与密钥」是另一类（服务端凭据，不进 localStorage），见「引擎凭据」。
_Avoid_: 选项、配置

### 引擎凭据

**引擎凭据（Engine credentials）**:
游戏用来对话与出图的那套「自家服务」配置（v1.10）：对话一组、图片一组，每组是「模式（沿用终端登录 / 自备密钥；不出图 / 自备服务）+ 服务目录 id + 服务地址 + 密钥 + 模型名（图片另有出图尺寸）」。落盘在 `~/.bunkiten/credentials.json`（目录 0700 / 文件 0600，明文），**不进剧本、不进世界线、不进导出包**；界面上只出现掩码（`sk-…4f2a`）。保存即时生效，但引擎要重启会话才读到新配置（设置屏的「立刻重启引擎」）。
_Avoid_: API key 管理、账号、登录（登录特指 grok CLI 的 `grok login` 那条路）

**服务目录（Provider catalog）**:
`shared/providers.mjs` 里那张预置服务表（id / 显示名 / 用途 / 地址 / 模型提示 / 说明），设置屏的两个下拉与服务端校验共用同一份——加一个服务是改数据，不是改代码。凡说 OpenAI 兼容协议的服务都在射程内（27 条，含国内的千帆/混元/星火/MiniMax，以及 Kimi·MiniMax·百炼 的国内与海外分站各一条）；长尾走「自定义」或自建网关（one-api / LiteLLM）。每条地址按官方文档核对，`note` 负责讲清「站点/地域要与密钥配套」这类文档里的坑。
_Avoid_: 适配器、插件、provider 实现

**出图工具（Media tool）**:
我们自建的 MCP 工具 `bunkiten-media__generate_image`（`server/media-mcp.mjs`）：按玩家自备的图片服务真的画一张，并**自己落盘**到 `presets/<剧本 id>/assets/…`（封面 `presets/<id>/cover.jpg`），回相对路径给引擎。引擎经 `search_tool` 找到它、`use_tool` 调用（SKILL【美术】的「出图工具优先（硬规则）」）；它不在或失败才回退内置 `image_gen`，两者都不行就静默跳过——**正文与选项绝不提图片**。
_Avoid_: 图像插件、画师、外部工具
